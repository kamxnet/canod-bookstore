{
  "name": "canod-bookstore",
  "version": "1.0.0",
  "private": true,
  "description": "CANOD independent Canadian publisher — an accessible, dependency-free bookstore for GitHub Pages.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/serve.mjs",
    "preview": "node scripts/serve.mjs",
    "check": "node scripts/check.mjs"
  },
  "engines": { "node": ">=20" }
}
